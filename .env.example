# Supabase (copier vers .env.local et remplir)
VITE_SUPABASE_URL=https://votre-projet.supabase.co
VITE_SUPABASE_ANON_KEY=votre_anon_key

# Mapbox — jeton public pk.* uniquement (exposé au navigateur, c’est voulu)
NEXT_PUBLIC_MAPBOX_TOKEN=
# (optionnel) même jeton, lisible côté serveur au build — pratique si tu évites NEXT_PUBLIC sur Vercel
# MAPBOX_PUBLIC_TOKEN=
